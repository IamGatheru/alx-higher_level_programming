#!/usr/bin/node
const fail = "Missing number of occurrences";
let count = process.argv.length;
let argument1 = process.argv

if (count <= 2 || argument1[1] < 0) {
  console.log(fail);
};
value = argument1[1];
for (let x = 0; x < argument1[2]; x++) {
    console.log(('C is fun'));
}
